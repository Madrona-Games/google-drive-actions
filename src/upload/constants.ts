/* eslint no-shadow: 0 */
/* eslint no-unused-vars: 0 */

export enum Inputs {
  FilePath = 'file-path',
  DestinationFolderID = 'destination-folder-id',
  DestinationFolderPath = 'destination-folder-path',
  GoogleServiceAccountCredentialsJson = 'google-service-account-credentials-json',
  GoogleDriveRootFolderId = 'google-drive-root-folder-id',
}

export enum Outputs {
  FileID = 'file-id',
}
